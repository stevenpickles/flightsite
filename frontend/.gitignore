# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
coverage
*.tsbuildinfo

# Copied from node_modules/maplibre-gl by scripts/copy-maplibre-worker.mjs
# (predev/prebuild) — see MapLibreMap.tsx's setWorkerUrl comment. Versioned
# by the copy script re-running on install/build, not by git, so it can
# never drift from whatever maplibre-gl version is actually installed.
/public/maplibre-gl-worker.mjs
/public/maplibre-gl-shared.mjs

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
